_schema-version: "3.2"
ID: Omnia
version: 0.0.1
modules:
- name: Omnia-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Omnia-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comravapedidopedidoventa.zip
      name: comravapedidopedidoventa
      target-path: resources/
    - artifacts:
      - Cobranzaomnia.zip
      name: Cobranzaomnia
      target-path: resources/
- name: comravapedidopedidoventa
  type: html5
  path: pedidoventa
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: Cobranzaomnia
  type: html5
  path: omnia
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Omnia-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Omnia-html5-srv
    service-plan: app-host
- name: Omnia-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Omnia-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
